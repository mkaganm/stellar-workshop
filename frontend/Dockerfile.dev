# Development Dockerfile for the Next.js frontend
FROM node:18-alpine

WORKDIR /app

ENV HOST=0.0.0.0

# Install dependencies separately to leverage Docker layer caching.
COPY package*.json ./
# Always install based on package.json to avoid lockfile drift in dev
RUN npm install

EXPOSE 3000

# Default command keeps hot reload running.
CMD ["npm", "run", "dev"]
