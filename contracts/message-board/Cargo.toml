[package]
name = "message-board"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = { version = "21.5.0" }

[dev-dependencies]
soroban-sdk = { version = "21.5.0" }
